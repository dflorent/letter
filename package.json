{
  "name": "letter",
  "version": "1.0.0",
  "description": "Editable and printable letter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server -f **/*.css -f **/*.js -f **/*.html --no-open"
  },
  "author": "Florent Desjardins <florent.desjardins@gmail.com>",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "browser-sync": "^2.12.3"
  },
  "dependencies": {
    "jquery": "^2.2.3",
    "moment": "^2.12.0",
    "normalize.css": "^4.1.1"
  }
}
